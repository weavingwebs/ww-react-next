import { LoadingState } from './TableResultsWithPlaceholder.stories';
import { Meta, Story, Title } from '@storybook/blocks';

<Meta title="Bootstrap/TableResultsWithPlaceholder" />
<Title>TableResultsWithPlaceholder</Title>

TableResultsWithPlaceholder is a wrapper for [TableResults](?path=/docs/components-tableresults--docs) that displays placeholder content when the table is loading.

See [Full Example With Paging](?path=/story/hooks-useurlfilters--full-example-with-paging) for a complete example.

<Story of={LoadingState} />
