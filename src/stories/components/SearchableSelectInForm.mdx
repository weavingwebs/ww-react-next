import { Canvas, Meta, Title } from '@storybook/blocks';
import { SearchableSelectInForm } from './SearchableSelectInForm.stories';

<Meta title="Components/SearchableSelectInForm" />
<Title>SearchableSelectInForm</Title>

#### The options are provided client-side (no data fetching).

<Canvas of={SearchableSelectInForm} />
