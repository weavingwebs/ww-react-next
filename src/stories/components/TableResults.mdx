import { ErrorState, Example, LoadingState } from './TableResults.stories';
import { Canvas, Meta, Story, Title } from '@storybook/blocks';

<Meta title="Components/TableResults" />
<Title>TableResults</Title>

TableResults is a component for rendering table results with standardised loading and error states.

See [Full Example With Paging](?path=/story/hooks-useurlfilters--full-example-with-paging) for a complete example.

Also see [TableResultsWithPlaceholder](?path=/docs/bootstrap-tableresultswithplaceholder--docs).

## Example

<Canvas of={Example} />

## Loading State

<Story of={LoadingState} />

## Error State

<Story of={ErrorState} />
